import * as path from "path";import * as webpack from "webpack";import * as webpackMerge from "webpack-merge";import * as baseConfig from "./base.config";const config: webpack.Configuration = webpackMerge(baseConfig, {    output: {        path: path.resolve(__dirname, "dist"),        filename: "[name].js",        publicPath: "/"    },    devtool: "cheap-module-eval-source-map",    devServer: {        inline: true,        hot: true    }});export default config;
