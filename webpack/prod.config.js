import * as path from "path";import * as webpack from "webpack";import * as webpackMerge from "webpack-merge";import * as ExtractTextPlugin from "extract-text-webpack-plugin";import * as UglifyJsPlugin from "uglifyjs-webpack-plugin";import * as baseConfig from "./base.config";const config: webpack.Configuration = webpackMerge(baseConfig, {    output: {        path: path.resolve(__dirname, "dist"),        filename: "[name].bundle.[chunkhash].js",        publicPath: "/dist"    },    devtool: "source-map",    plugins: [        //Extract imported CSS into own file        new ExtractTextPlugin("[name].bundle.[chunkhash].css"),        //Minify JS        new UglifyJsPlugin({ sourceMap: true, compress: true }),        //Minify CSS        new webpack.LoaderOptionsPlugin({ minimize: true }),        new webpack.DefinePlugin({            "process.env.NODE_ENV": JSON.stringify("production")        })    ]});export default config;
